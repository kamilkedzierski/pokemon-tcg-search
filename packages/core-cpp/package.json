{
  "name": "@pokemon-search/core-cpp",
  "version": "0.1.0",
  "description": "Portable C++ core library for Pokemon card search",
  "license": "MIT",
  "files": [
    "CMakeLists.txt",
    "include",
    "src",
    "tests",
    "README.md",
    "*.podspec"
  ],
  "scripts": {
    "build": "cmake -S . -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build",
    "test": "cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON && cmake --build build && ctest --test-dir build --output-on-failure",
    "lint": "echo 'No C++ lint configured yet'",
    "clean": "rm -rf build"
  }
}
